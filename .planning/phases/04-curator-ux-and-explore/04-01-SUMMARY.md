---
phase: 04-curator-ux-and-explore
plan: 01
subsystem: api
tags: [sparql, aop-wiki, ke-detail, service-container, precompute]

# Dependency graph
requires:
  - phase: 03-stable-public-rest-api
    provides: api.py blueprint structure, ServiceContainer lazy-load pattern, ke_metadata.json

provides:
  - scripts/precompute_ke_aop_membership.py: single-SPARQL-call script writing data/ke_aop_membership.json
  - ServiceContainer.ke_aop_membership: lazy property loading ke_aop_membership.json
  - ServiceContainer.ke_metadata_index: lazy dict-index keyed by KElabel for O(1) lookup
  - GET /api/ke_detail/<ke_id>: local-data endpoint returning KE title, description, biolevel, ke_page, aop_membership

affects:
  - 04-02-curator-ux (KE context panel depends on /api/ke_detail)
  - any downstream KE context work

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Precompute pattern: single bulk SPARQL -> JSON artifact -> lazy-load via ServiceContainer"
    - "set_models injection: ke_aop_membership_data kwarg added to api.py set_models()"

key-files:
  created:
    - scripts/precompute_ke_aop_membership.py
  modified:
    - src/services/container.py
    - src/blueprints/api.py
    - app.py

key-decisions:
  - "Single bulk SPARQL for all AOP->KE relationships (not per-KE SPARQL) — locked KE-01 decision, prevents live SPARQL per KE selection"
  - "ke_aop_membership.json gitignored (data/*.json) — generated artifact regenerated via precompute script, not stored in repo"
  - "Linear scan over ke_metadata list (~1561 entries) for KE lookup — fast enough, avoids extra index complexity in endpoint; ke_metadata_index property available for future O(1) needs"
  - "<path:ke_id> URL converter used so 'KE 55' URL-encoded as 'KE%2055' decodes correctly"

patterns-established:
  - "ServiceContainer lazy property pattern: _ke_aop_membership = None in __init__, guarded load in property, warning on load failure"

requirements-completed: [KE-01]

# Metrics
duration: 4min
completed: 2026-02-21
---

# Phase 4 Plan 01: KE AOP Membership Data Infrastructure Summary

**Single-SPARQL precompute script + ServiceContainer lazy properties + no-SPARQL /api/ke_detail/<ke_id> endpoint serving KE title, description, biolevel, ke_page, and AOP membership from local JSON**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-21T13:38:53Z
- **Completed:** 2026-02-21T13:43:51Z
- **Tasks:** 2
- **Files modified:** 4 (1 created + 3 modified)

## Accomplishments

- Precompute script fetches all 1567 KEs x 3614 AOP membership rows in one SPARQL call, writing data/ke_aop_membership.json
- ServiceContainer gains `ke_aop_membership` and `ke_metadata_index` lazy properties following established container pattern
- `/api/ke_detail/<ke_id>` endpoint serves all 6 KE-01 required fields from local JSON — zero live SPARQL on hit
- All 66 existing tests pass with no regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Write precompute_ke_aop_membership.py script** - `a64618a` (feat)
2. **Task 2: Add ServiceContainer properties and /api/ke_detail endpoint** - `769a59f` (feat)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified

- `scripts/precompute_ke_aop_membership.py` - Single bulk SPARQL -> groups by KE label -> writes data/ke_aop_membership.json
- `src/services/container.py` - Added ke_aop_membership and ke_metadata_index lazy properties; added `_ke_aop_membership` and `_ke_metadata_index` to __init__
- `src/blueprints/api.py` - Added ke_aop_membership global, updated set_models() signature, added GET /api/ke_detail/<path:ke_id> endpoint
- `app.py` - Pass `ke_aop_membership_data=services.ke_aop_membership` to set_api_models

## Decisions Made

- Single bulk SPARQL for all AOP->KE relationships (per locked KE-01 decision prohibiting live SPARQL per KE selection)
- ke_aop_membership.json gitignored (data/*.json pattern) — generated artifact, regenerated by precompute script
- Linear scan over ~1561 ke_metadata entries for KE detail lookup — O(n) is fast enough; ke_metadata_index property available for future O(1) optimization
- `<path:ke_id>` URL converter ensures "KE%2055" decodes to "KE 55" correctly in Flask routing

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None — SPARQL endpoint was available and returned complete data (1567 KEs, 3614 AOP-KE rows). Test stash pop revealed unrelated uncommitted work from a future plan; those files were restored to HEAD before committing.

## User Setup Required

None - no external service configuration required. The precompute script must be re-run when AOP-Wiki data changes (`python scripts/precompute_ke_aop_membership.py`), but this is an operator task, not user configuration.

## Next Phase Readiness

- `/api/ke_detail/<ke_id>` endpoint ready for 04-02 KE context panel to consume
- AOP membership data now served from local JSON — no per-request SPARQL latency
- `ke_metadata_index` available in ServiceContainer for any code needing O(1) KE lookup

---
*Phase: 04-curator-ux-and-explore*
*Completed: 2026-02-21*

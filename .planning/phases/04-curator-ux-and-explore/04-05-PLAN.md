---
phase: 04-curator-ux-and-explore
plan: 05
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
  - 04-04
files_modified: []
autonomous: false
requirements:
  - EXPLO-01
  - EXPLO-02
  - EXPLO-03
  - EXPLO-05
  - EXPLO-06
  - KE-01

must_haves:
  truths:
    - "All five Phase 4 success criteria are visually verified as working"
    - "No regressions in existing mapping/submission workflow"
---

<objective>
Human verification checkpoint for all Phase 4 features. Tests the complete curator UX and explore suite in the browser to confirm all five success criteria from the roadmap are met.

Purpose: Confirm the phase is shippable before closing Phase 4.
Output: Curator confirms all success criteria pass; phase marked complete in STATE.md.
</objective>

<execution_context>
@/home/marvin/.claude/get-shit-done/workflows/execute-plan.md
@/home/marvin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase 4 UAT — verify all curator UX and explore features</name>
  <files></files>
  <action>No automated changes. This is a human verification checkpoint for all Phase 4 work.</action>
  <what-built>
Complete Phase 4 feature set:
- KE-01: Unified collapsible KE context panel (title, description, AOP membership, biolevel badge, AOP-Wiki link)
- EXPLO-01: Explore page AOP filter (Select2 search-as-you-type, live filtering)
- EXPLO-02: Explore page confidence filter (toggle buttons, live filtering)
- EXPLO-03: Coverage Gaps tab showing unmapped KEs per AOP with Map button
- EXPLO-05: Public /stats page with metric cards and confidence breakdown table
- EXPLO-06: Filtered export from /stats (Download JSON and Download CSV buttons)
  </what-built>
  <how-to-verify>
Start the server: pkill -f "python.*app.py" 2>/dev/null; python app.py &amp;

**Test 1 — KE context panel (KE-01):**
1. Visit http://localhost:5000
2. Select any KE from the KE dropdown (e.g., search for "KE 55")
3. Confirm: a collapsible panel appears with KE title prominently at top, biolevel badge, description, AOP membership list (or "no AOPs" message), and "View on AOP-Wiki →" link
4. Click the panel summary line — it collapses
5. Click again — it re-expands
6. In browser Network tab: confirm /api/ke_detail/ was called, NOT /api/ke_context/

**Test 2 — URL pre-fill (KE-01 + EXPLO-03 Map button integration):**
1. Visit http://localhost:5000/?ke_id=KE%2055
2. Confirm: KE 55 is pre-selected in the dropdown and context panel auto-loads

**Test 3 — Explore page AOP filter (EXPLO-01):**
1. Visit http://localhost:5000/explore
2. On the KE-WP Mappings tab: select an AOP from the AOP dropdown
3. Confirm: table reloads with only mappings from that AOP; AOP chip appears above table
4. Click × on the AOP chip — filter cleared, all mappings shown again

**Test 4 — Explore page confidence filter (EXPLO-02):**
1. On the KE-WP Mappings tab: click "High" confidence button
2. Confirm: table reloads with only High-confidence mappings; "Confidence: High" chip appears
3. Click "All" button — table shows all mappings again

**Test 5 — Combined filters (EXPLO-01 + EXPLO-02):**
1. Select an AOP AND click "High" confidence
2. Confirm: both chips appear; table shows only High-confidence mappings for that AOP
3. Click "Clear all" — both filters cleared

**Test 6 — Coverage Gaps tab (EXPLO-03):**
1. Click the "Coverage Gaps" tab
2. Select an AOP from the AOP dropdown
3. Confirm: table loads with KE ID, KE Title, Biological Level, and a "Map" button for each unmapped KE
4. Click the "Map" button for any row — navigates to /?ke_id=KE%20NNN and pre-selects that KE

**Test 7 — Stats page (EXPLO-05):**
1. Visit http://localhost:5000/stats (WITHOUT being logged in, or in incognito)
2. Confirm: page loads (200, no login redirect)
3. Confirm: three stat cards visible with KE-WP count, KE-GO count, and Total
4. Confirm: confidence breakdown table shows High/Medium/Low rows

**Test 8 — Filtered export (EXPLO-06):**
1. On /stats, select an AOP from the AOP dropdown
2. Confirm: Download JSON href updates to include aop_id=... param
3. Click "Download CSV" — browser downloads a CSV file
4. Open CSV — confirm it has column headers and data rows matching the filter

**Test 9 — No regressions:**
1. Visit http://localhost:5000 — mapping workflow still works (KE select, suggestions, form submit)
2. Visit http://localhost:5000/explore — KE-GO Mappings tab still works (server-rendered rows)
3. Run: make test — all tests pass
  </how-to-verify>
  <verify>All 9 manual tests pass (see how-to-verify). make test passes.</verify>
  <done>All Phase 4 roadmap success criteria confirmed by human curator. No regressions.</done>
  <resume-signal>Type "approved" if all tests pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All Phase 4 success criteria confirmed working:
1. Curator sees KE description, AOP context, and biological level without leaving the page
2. Explore page filterable by AOP
3. Explore page filterable by confidence level
4. Coverage gap view shows unmapped KEs per AOP
5. Dataset metrics dashboard with filtered export
</verification>

<success_criteria>
All 5 roadmap success criteria for Phase 4 verified by human curator.
make test passes with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-curator-ux-and-explore/04-05-SUMMARY.md`

Update STATE.md: Phase 4 complete, ready for Phase 5.
</output>
